# Home Assistant (OpenClaw)

## URL

- HA_URL: https://pu5vbx3qmqg5v8kovlb3vgp79etd8hdj.ui.nabu.casa/

## Token

For security, **do not store HA_TOKEN in this repo/workspace**.

OpenClaw is configured via `~/.openclaw/openclaw.json` under:

- `skills.entries.homeassistant.env.HA_URL`
- `skills.entries.homeassistant.env.HA_TOKEN`

If you need to rotate the token:
Home Assistant → Profile → Long-Lived Access Tokens → Create/Replace token.
